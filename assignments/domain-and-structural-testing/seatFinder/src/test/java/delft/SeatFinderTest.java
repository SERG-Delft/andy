package delft;

import static org.assertj.core.api.Assertions.*;
import static org.junit.jupiter.api.Assertions.*;

import java.util.*;
import java.util.stream.*;
import org.assertj.core.data.*;
import org.junit.jupiter.api.*;
import org.junit.jupiter.params.*;
import org.junit.jupiter.params.provider.*;

class SeatFinderTest {

    @Test
    void t1() {
        
    }

    // Some utility methods that you can use to write the test

    /** 
     * To create an array of doubles inline, you should do, e.g.:
     * new double[] { 1.0, 2.5 }
     * The method below is just a helper that lets you do:
     * double[] arrayForTheTest = arrayOf(1.0, 2.5);
     * Use any of them!
     */
    private static double[] arrayOf(double... values) {
        return values;
    }

    /**
     * The same for booleans!
     */
    private static boolean[] arrayOf(boolean... values) {
        return values;
    }
    
}
